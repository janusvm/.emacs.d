;; set-prog-python.el : settings for Python development
;; -----------------------------------------------------------------------------

(use-package elpy
  :config
  (elpy-enable))

(provide 'set-prog-python)
